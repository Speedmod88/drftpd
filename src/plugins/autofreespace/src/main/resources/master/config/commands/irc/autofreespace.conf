dupe2 {
plugin org.drftpd.autofreespace.master
class AutoFreeSpaceCommands
method doSITE_DUPE2
perms *
limit 10
# When set to false it will only show publicly available paths, if true it will honor privpath rules.
# Default: [false]
observe.privpath false
help Shows completed duplicate release variants using AutoFreeSpace Dupe2 rules.
help.specific ${command} <release-name-or-path>
input all
output source
}
